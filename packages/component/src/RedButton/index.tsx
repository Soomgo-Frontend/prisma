import styles from '@/RedButton/RedButton.module.css';

const RedButton = () => {
  console.log('button button');
  return <button className={styles.buttonBase} >Button</button>;
};

export default RedButton;